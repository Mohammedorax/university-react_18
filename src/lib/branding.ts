/**
 * مسار الشعار الافتراضي للجامعة (نسخة من `assets/logo.png` في `public/assets/` لخدمة Vite).
 * يُستخدم عند عدم وجود شعار مخصص في التخزين المحلي.
 */
export const DEFAULT_UNIVERSITY_LOGO_SRC = `${import.meta.env.BASE_URL}assets/logo-white.png`
